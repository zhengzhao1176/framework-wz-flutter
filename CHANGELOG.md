# Changelog

## 0.1.0 — initial port

- M0 — Flutter skeleton (pubspec, lints, directory structure)
- M1 — `core/` (theme tokens, errors, storage, network, router skeleton, common widgets)
- M2 — data + mock layer (dio with mock interceptor + JSON fixtures + auth repository)
- M3 — app shell (sidebar with multi-level dynamic menu + tabs with state preservation + breadcrumb + responsive layout)
- M4 — six feature pages (dashboard, charts, markdown / richtext editors, JSON tree view, table with CSV export, permission demo, error pages)
- M5 — tests (unit + widget + integration), CI workflow, documentation

Reference plan: [`task/`](task/README.md).
Test plan: [`task/04-test-plan.md`](task/04-test-plan.md).
